(()=>{
'use strict';
const $id=id=>document.getElementById(id);
const safe=v=>typeof esc==='function'?esc(v):String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
const dateFmt=v=>typeof fmt==='function'?fmt(v):(v||'N/A');
function tracked(){return (db?.aeds||[]).filter(a=>typeof isInventoryTracked==='function'?isInventoryTracked(a):true)}
function groups(){return [...new Set(tracked().map(a=>a.group||a.location||'Unassigned'))].sort((a,b)=>a.localeCompare(b))}
function period(){return {year:Number($id('reportYear')?.value||new Date().getFullYear()),quarter:Number($id('reportQuarter')?.value||1)}}
function periodChecks(a,year,quarter){return (a.checks||[]).filter(c=>Number(c.year)===year&&Number(c.quarter)===quarter).sort((x,y)=>String(y.date||'').localeCompare(String(x.date||'')))}
function periodDate(v,year,quarter){if(!v)return false;const d=new Date(String(v).slice(0,10)+'T12:00:00');return !isNaN(d)&&d.getFullYear()===year&&(Math.floor(d.getMonth()/3)+1)===quarter}
function selectedIds(){return [...document.querySelectorAll('.muster-aed:checked')].map(x=>x.value)}
function renderPicker(){const mode=$id('musterMode')?.value||'individual',wrap=$id('musterPicker');if(!wrap)return;if(mode==='group'){wrap.innerHTML='<label>Group<select id="musterGroup">'+groups().map(g=>'<option value="'+safe(g)+'">'+safe(g)+'</option>').join('')+'</select></label>'}else if(mode==='individual'){wrap.innerHTML='<label>AED<select id="musterIndividual">'+tracked().map(a=>'<option value="'+a.id+'">'+safe(a.location)+(a.descriptor?' — '+safe(a.descriptor):'')+' · '+safe(a.serial)+'</option>').join('')+'</select></label>'}else{wrap.innerHTML='<div class="card" style="max-height:320px;overflow:auto"><div class="bar" style="margin-bottom:6px"><b>Select AEDs</b><div class="actions" style="margin-top:0"><button type="button" id="musterAll">All</button><button type="button" id="musterNone">None</button></div></div>'+tracked().map(a=>'<label class="check"><input class="muster-aed" type="checkbox" value="'+a.id+'"><span>'+safe(a.location)+(a.descriptor?' — '+safe(a.descriptor):'')+' <span class="muted">'+safe(a.serial)+'</span></span></label>').join('')+'</div>';$id('musterAll').onclick=()=>document.querySelectorAll('.muster-aed').forEach(x=>x.checked=true);$id('musterNone').onclick=()=>document.querySelectorAll('.muster-aed').forEach(x=>x.checked=false)}}
function choose(){const mode=$id('musterMode').value;if(mode==='group'){const g=$id('musterGroup').value;return tracked().filter(a=>(a.group||a.location||'Unassigned')===g)}if(mode==='individual'){const id=$id('musterIndividual').value;return tracked().filter(a=>a.id===id)}const ids=new Set(selectedIds());return tracked().filter(a=>ids.has(a.id))}
function status(a){return safe(a.status||'In Service')}
function daysTo(v){if(!v)return null;const d=new Date(String(v).slice(0,10)+'T12:00:00'),n=new Date();n.setHours(12,0,0,0);return isNaN(d)?null:Math.ceil((d-n)/86400000)}
function components(a){const x=[{name:'Adult pads',date:a.adult},{name:'Battery',date:a.battery}];if(a.pedConfig!=='N/A')x.splice(1,0,{name:'Pediatric pads',date:a.ped});return x}
function healthData(units,year,quarter){let oos=0,expired=0,due180=0,missing=0,checksComplete=0,deployments=0,shocks=0,issues=[];units.forEach(a=>{const label=a.location+(a.descriptor?' — '+a.descriptor:'');if(String(a.status||'In Service')!=='In Service'){oos++;issues.push(label+': '+(a.status||'Out of Service'))}if(periodChecks(a,year,quarter).length)checksComplete++;components(a).forEach(c=>{const d=daysTo(c.date);if(d===null){missing++;issues.push(label+': '+c.name+' expiration not recorded')}else if(d<0){expired++;issues.push(label+': '+c.name+' expired '+dateFmt(c.date))}else if(d<=180){due180++;issues.push(label+': '+c.name+' expires '+dateFmt(c.date))}});(a.deps||[]).filter(d=>periodDate(d.date,year,quarter)).forEach(d=>{deployments++;if(d.shock)shocks++})});const attention=oos+expired+missing,health=attention?'ATTENTION REQUIRED':due180?'WATCH':'GOOD',healthText=attention?'One or more AEDs have an out-of-service status, expired component, or missing expiration data requiring review.':due180?'All selected AEDs are currently serviceable based on recorded status, but one or more components expire within 180 days.':'No out-of-service AEDs, expired components, missing component dates, or component expirations within 180 days were identified in the selected records.';return {oos,expired,due180,missing,checksComplete,deployments,shocks,issues:[...new Set(issues)],health,healthText}}
function executiveSummary(units,year,quarter,mode,groupName){const h=healthData(units,year,quarter),scope=mode==='group'?groupName:mode==='individual'?(units[0].location+(units[0].descriptor?' — '+units[0].descriptor:'')):'Selected AEDs',complete=h.checksComplete===units.length;const issues=h.issues.length?'<h3 style="margin:14px 0 6px">Items Requiring Attention / Watch</h3><ul style="margin:0;padding-left:22px">'+h.issues.slice(0,12).map(x=>'<li>'+safe(x)+'</li>').join('')+(h.issues.length>12?'<li>+'+(h.issues.length-12)+' additional item(s) detailed in this report.</li>':'')+'</ul>':'<p><b>Attention items:</b> None identified from the recorded inventory data.</p>';return '<section style="min-height:9.4in;page-break-after:always;break-after:page"><div class="report-box"><h2 style="margin-top:0;color:var(--n)">Executive Summary</h2><p><b>Scope:</b> '+safe(scope)+' · Q'+quarter+' '+year+' · '+units.length+' AED'+(units.length===1?'':'s')+'</p><div class="report-grid"><div class="report-box"><h3 style="margin-top:0">Overall Health</h3><div style="font-size:22px;font-weight:800">'+safe(h.health)+'</div><p>'+safe(h.healthText)+'</p></div><div class="report-box"><h3 style="margin-top:0">Quarterly Status</h3><div class="kv"><b>Inspections completed</b><div>'+h.checksComplete+' / '+units.length+'</div></div><div class="kv"><b>Quarter complete</b><div>'+(complete?'Yes':'No')+'</div></div></div></div><div class="report-grid"><div class="report-box"><h3 style="margin-top:0">Readiness</h3><div class="kv"><b>Out of service</b><div>'+h.oos+'</div></div><div class="kv"><b>Expired components</b><div>'+h.expired+'</div></div><div class="kv"><b>Expiration ≤180 days</b><div>'+h.due180+'</div></div><div class="kv"><b>Missing expiration dates</b><div>'+h.missing+'</div></div></div><div class="report-box"><h3 style="margin-top:0">Activity This Quarter</h3><div class="kv"><b>Deployments</b><div>'+h.deployments+'</div></div><div class="kv"><b>Shock-delivery events</b><div>'+h.shocks+'</div></div></div></div>'+issues+'<p class="muted" style="margin-top:18px">Health is generated from the current status, recorded component expiration dates, quarterly inspection records, and deployment history contained in this report.</p></div></section>'}
async function reportPdfBlob(content,filename){if(typeof html2pdf!=='function')throw new Error('PDF generator is not available.');const host=document.createElement('div');host.style.position='fixed';host.style.left='-10000px';host.style.top='0';host.style.width='800px';host.style.background='#fff';host.innerHTML=content;document.body.appendChild(host);try{const opt={margin:[0.35,0.35,0.35,0.35],filename,image:{type:'jpeg',quality:0.98},html2canvas:{scale:2,useCORS:true,backgroundColor:'#ffffff'},jsPDF:{unit:'in',format:'letter',orientation:'portrait'},pagebreak:{mode:['css','legacy']}};return await html2pdf().set(opt).from(host).outputPdf('blob')}finally{host.remove()}}
async function saveReportPdf(content,filename){const blob=await reportPdfBlob(content,filename);const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=filename;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1500)}
async function shareReportPdf(content,filename,title,summary){const blob=await reportPdfBlob(content,filename);const file=new File([blob],filename,{type:'application/pdf'});if(navigator.share&&navigator.canShare&&navigator.canShare({files:[file]})){await navigator.share({title,text:summary,files:[file]});return}const url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download=filename;document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1500);alert('PDF sharing is not supported on this device, so the PDF was downloaded instead.')}
function reportSection(a,year,quarter){const checks=periodChecks(a,year,quarter),deps=(a.deps||[]).filter(d=>periodDate(d.date,year,quarter)).sort((x,y)=>String(y.date||'').localeCompare(String(x.date||'')));const audit=(db.audit||[]).filter(e=>e.id===a.id&&periodDate(e.time,year,quarter)&&/Status Change|Return|Deployment/i.test(e.type||'')).sort((x,y)=>String(y.time||'').localeCompare(String(x.time||'')));const checkRows=checks.length?checks.map(c=>'<tr><td>'+dateFmt(c.date)+'</td><td>'+safe(c.inspector||'N/A')+(c.employeeNumber?' · #'+safe(c.employeeNumber):'')+'</td><td>'+safe(c.auditResult||c.status||'Completed')+'</td></tr>').join(''):'<tr><td colspan="3">No quarterly inspection recorded.</td></tr>';const depRows=deps.length?deps.map(d=>'<tr><td>'+dateFmt(d.date)+'</td><td>'+safe(d.report||d.agencyReport||'N/A')+'</td><td>'+(d.shock?'Shock delivered':'No shock')+(d.adultPadsUsed?' · Adult pads used':'')+(d.pediatricPadsUsed?' · Pediatric pads used':'')+(d.rts?' · Returned to service':'')+'</td></tr>').join(''):'<tr><td colspan="3">No deployments recorded.</td></tr>';const auditRows=audit.length?audit.map(e=>'<tr><td>'+dateFmt(String(e.time||'').slice(0,10))+'</td><td>'+safe(e.type)+'</td><td>'+safe(e.detail||'')+'</td></tr>').join(''):'<tr><td colspan="3">No status/return-to-service events recorded.</td></tr>';return '<section class="report-box" style="margin:0 0 18px;break-inside:avoid"><h2 style="margin:0;color:var(--n)">'+safe(a.location)+(a.descriptor?' — '+safe(a.descriptor):'')+'</h2><p class="muted" style="margin:4px 0 12px">Serial: '+safe(a.serial)+' · Group: '+safe(a.group||'Unassigned')+' · Current Status: '+status(a)+'</p><div class="report-grid"><div class="report-box"><h3>Current Components</h3><div class="kv"><b>Adult Pads</b><div>'+dateFmt(a.adult)+'</div></div>'+(a.pedConfig!=='N/A'?'<div class="kv"><b>Pediatric Pads</b><div>'+dateFmt(a.ped)+'</div></div>':'')+'<div class="kv"><b>Battery</b><div>'+dateFmt(a.battery)+'</div></div></div><div class="report-box"><h3>Quarterly Inspection</h3><table class="summary-table"><thead><tr><th>Date</th><th>Inspector</th><th>Result</th></tr></thead><tbody>'+checkRows+'</tbody></table></div></div><h3>Deployments</h3><table class="summary-table"><thead><tr><th>Date</th><th>Report #</th><th>Event</th></tr></thead><tbody>'+depRows+'</tbody></table><h3>Status / Return-to-Service History</h3><table class="summary-table"><thead><tr><th>Date</th><th>Event</th><th>Details</th></tr></thead><tbody>'+auditRows+'</tbody></table></section>'}
function build(){const units=choose();if(!units.length){alert('Select at least one AED.');return}const {year,quarter}=period(),mode=$id('musterMode').value,groupName=mode==='group'?($id('musterGroup')?.value||'Unassigned'):'',title=mode==='group'?groupName+' AED Report':mode==='individual'?'Individual AED History':'Selected AED Report',subtitle=mode==='group'?'Gladstone Fire / EMS · Group: '+groupName+' · Q'+quarter+' '+year+' · '+units.length+' AED'+(units.length===1?'':'s'):'Gladstone Fire / EMS · Q'+quarter+' '+year+' · '+units.length+' AED'+(units.length===1?'':'s');const area=$id('reportPrintArea');if(!area){alert('Report print area is unavailable.');return}const generated='Generated '+new Date().toLocaleString()+'.',header='<div class="report-header"><img src="gfd-patch.jpg" alt="Gladstone Fire EMS"><div class="report-title"><h1>'+safe(title)+'</h1><p>'+safe(subtitle)+'</p></div></div>',exec=executiveSummary(units,year,quarter,mode,groupName),details=units.map(a=>reportSection(a,year,quarter)).join(''),core=header+exec+details+'<div class="report-foot">'+safe(generated)+'</div>',pdfContent='<div class="report-sheet">'+core+'</div>',slug=String(mode==='group'?groupName:title).replace(/[^a-z0-9]+/ig,'-').replace(/^-+|-+$/g,'').toLowerCase()||'aed-report',pdfFilename=slug+'-q'+quarter+'-'+year+'.pdf',summary=title+' · Q'+quarter+' '+year+' · '+units.length+' AED'+(units.length===1?'':'s');area.innerHTML='<div class="report-sheet">'+core+'<div class="report-toolbar"><button type="button" class="primary" id="musterPdf">Export PDF</button><button type="button" id="musterSharePdf">Share PDF</button><button type="button" id="musterClose">Close</button></div></div>';area.style.display='block';document.querySelectorAll('main>.view').forEach(v=>v.style.display='none');area.scrollIntoView({behavior:'smooth',block:'start'});$id('musterPdf').onclick=async()=>{const b=$id('musterPdf');b.disabled=true;b.textContent='Creating PDF…';try{await saveReportPdf(pdfContent,pdfFilename)}catch(e){alert(e.message||'Unable to create PDF.')}finally{b.disabled=false;b.textContent='Export PDF'}};$id('musterSharePdf').onclick=async()=>{const b=$id('musterSharePdf');b.disabled=true;b.textContent='Preparing PDF…';try{await shareReportPdf(pdfContent,pdfFilename,title,summary)}catch(e){if(!e||e.name!=='AbortError')alert(e.message||'Unable to share PDF.')}finally{b.disabled=false;b.textContent='Share PDF'}};$id('musterClose').onclick=()=>{area.style.display='none';document.querySelectorAll('main>.view').forEach(v=>v.style.display='');if(typeof goView==='function')goView('reports')}}
function install(){const sec=$id('reports');if(!sec||$id('musterBuilder'))return;const box=document.createElement('div');box.id='musterBuilder';box.className='card';box.innerHTML='<div class="bar"><div><h2 style="margin:0">Build AED Report</h2><p class="muted" style="margin:3px 0 0">Generate a report for one AED, an entire group, or any selected AEDs.</p></div></div><div class="grid"><label>Report Type<select id="musterMode"><option value="individual">Individual AED</option><option value="group">AED Group</option><option value="selected">Selected AEDs</option></select></label><div id="musterPicker"></div></div><div class="actions"><button type="button" class="primary" id="musterBuild">Generate Report</button></div>';sec.insertBefore(box,sec.firstChild);$id('musterMode').onchange=renderPicker;$id('musterBuild').onclick=build;renderPicker()}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',install);else install();
})();